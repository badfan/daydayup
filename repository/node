maven仓库创建
关联的lib在startup中的build.gradle

apply plugin: 'maven'
uploadArchives{
    repositories.mavenDeployer{
        // 配置本地仓库路径，项目根目录下的repository目录中
        repository(url: uri('../repository'))
        pom.groupId = "com.ff.startup"// 唯一标识（通常为模块包名，也可以任意）
        pom.artifactId = "startup" // 项目名称（通常为类库模块名称，也可以任意）
        pom.version = "1.0.0" // 版本号
    }
}

然后编译后在Gradle视图:startup/Tasks/upload/uploadArchives
双击即可在repository(url: uri('../repository'))目录下生成仓库arr包引用
compile group: 'com.ff.startup', name: 'startup', version: '1.0.0'